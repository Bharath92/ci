language: none

build:
  ci: 
    - docker ps
    - docker pull bharath92/gitissues
integrations:    
  hub:
      #Docker integration
    - integrationName: docker
      type: dockerRegistryLogin
    
