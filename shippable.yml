language: none

runtime:
  nodePool: default-restricted

build:
  ci: 
    - docker ps
    - docker push bharath92/gitissues:not
integrations:    
  hub:
      #Docker integration
    - integrationName: invalid-docker
      type: dockerRegistryLogin
    
