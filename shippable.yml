language: none

runtime:
  nodePool: default-restricted

build:
  ci: 
    - docker ps
    - docker build -t=bharath92/gitissues:not .
    - docker push bharath92/gitissues:not
integrations:    
  hub:
      #Docker integration
    - integrationName: docker
      type: dockerRegistryLogin
    
