language: none

runtime:
  nodePool: restricted

build:
  ci: 
    - docker ps
    - docker build bharath92/gitissues:not .
    - docker push bharath92/gitissues:not
integrations:    
  hub:
      #Docker integration
    - integrationName: docker
      type: dockerRegistryLogin
    
