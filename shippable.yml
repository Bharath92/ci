language: none

runtime:
  nodePool: default-restricted

build:
  pre_ci:
    - echo hi
  ci: 
    - docker ps -a
#    - docker pull bharath92/gitissues:not
integrations:    
  hub:
      #Docker integration
    - integrationName: docker
      type: dockerRegistryLogin
    
